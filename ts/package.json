{
  "name": "eth-prices",
  "version": "0.0.2",
  "description": "A library for fetching Ethereum prices.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "import": "./index.js"
    }
  },
  "files": [
    "pkg",
    "index.js",
    "index.d.ts"
  ],
  "scripts": {
    "build": "cargo build --target wasm32-unknown-unknown --release --manifest-path ../Cargo.toml -p eth-prices && wasm-bindgen --target web --out-dir pkg ../target/wasm32-unknown-unknown/release/eth_prices.wasm",
    "test": "pnpm build && vitest run"
  },
  "keywords": [],
  "author": "",
  "license": "LGPL-3.0",
  "packageManager": "pnpm@10.15.0",
  "devDependencies": {
    "@types/node": "^25.5.0",
    "vitest": "^3.0.0"
  }
}
