{
  "name": "eth-prices",
  "version": "0.0.2",
  "type": "module",
  "description": "JavaScript bindings for eth-prices wasm",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src",
    "pkg"
  ],
  "scripts": {
    "build:wasm": "pnpm -C ../pkg build",
    "test": "pnpm run build:wasm && vitest run"
  },
  "license": "LGPL-3.0",
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite-plugin-wasm": "^3.5.0",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@10.15.0"
}
