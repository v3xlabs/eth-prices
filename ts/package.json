{
  "name": "eth-prices",
  "version": "0.0.2",
  "description": "A library for fetching Ethereum prices.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "pkg",
    "dist"
  ],
  "scripts": {
    "build": "cargo build --target wasm32-unknown-unknown --release --manifest-path ../Cargo.toml -p eth-prices && wasm-bindgen --target bundler --out-dir pkg ../target/wasm32-unknown-unknown/release/eth_prices.wasm && tsc",
    "test": "pnpm build && vitest run"
  },
  "keywords": [],
  "author": "",
  "license": "LGPL-3.0",
  "packageManager": "pnpm@10.15.0",
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite-plugin-wasm": "^3.5.0",
    "vitest": "^3.0.0"
  }
}
