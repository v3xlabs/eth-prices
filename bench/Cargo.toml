[package]
name = "eth-prices-bench"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
alloy = { version = "2", features = ["json-rpc", "provider-http", "reqwest-rustls-tls"], default-features = false }
eth-prices = { path = "../cargo" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
tower = "0.5"
